# SubDl 项目规则

## 项目概述
SubDl 是一个自动化订阅管理工具：下载 Clash 订阅 → 转换为 sing-box 格式 → 合并模板配置 → 上传到 GitHub Gist。
运行环境为 GitHub Actions，由 cron 定时触发。

详细架构文档见 `ARCHITECTURE.md`，本文件聚焦 AI 编码时需要遵守的规范和约束。

项目完全运行在 GitHub Actions CI 上，本地仅用于编写代码，不在本地运行主流程。

## 技术栈
- Python 3.12+（主逻辑）
- Node.js（Clash → sing-box 转换，通过 subprocess 调用 convert.mjs）
- Python 依赖：json5（JSONC 解析）、pyyaml（ruleset_convert.py 规则源解析）
- Node.js 依赖：jsdom（DOM 环境）、Sub-Store proxy-utils（协议解析与转换）
- 网络请求：纯 urllib（零第三方网络依赖）
- JSONC 模板配置（支持注释）

## 环境变量

### 必需
| 变量 | 说明 | 使用文件 |
|------|------|----------|
| `GH_TOKEN` | GitHub Personal Access Token（gist 权限） | update_subscriptions.py |
| `GIST_ID` | 目标 Gist ID | update_subscriptions.py |
| `SUB_URL` | 主订阅链接（纯 URL） | update_subscriptions.py |
| `SING_BOX_BIN` | sing-box 官方二进制路径（配置校验用） | update_subscriptions.py |

### 可选
| 变量 | 说明 | 默认值 | 使用文件 |
|------|------|--------|----------|
| `SUB_URL_1` ~ `SUB_URL_9` | 更多常规订阅（纯 URL） | — | update_subscriptions.py |
| `GIST_URL` / `GIST_URL_1` ~ `GIST_URL_9` | Gist 订阅（纯 URL） | — | update_subscriptions.py |
| `SING_BOX_REF1ND_BIN` | sing-box reF1nd 二进制路径（providers 配置校验用） | 回退到官方版 | update_subscriptions.py |
| `WORKERS` | 并行下载线程数 | `8` | update_subscriptions.py |

### SUB_URL / GIST_URL 环境变量约定
- `providers.jsonc` 中 provider 的 `url` 字段为 `$ENV_VAR` 占位符（如 `$SUB_URL_1`、`$GIST_URL`）
- `SUB_URL` / `SUB_URL_N` — 常规订阅，provider URL 指向原始订阅地址
- `GIST_URL` / `GIST_URL_N` — Gist 订阅，provider URL 指向 Gist 上已转换的 sing-box 文件
- 环境变量值为纯 URL，不再包含订阅名称
- 订阅名称由 `providers.jsonc` 中 provider 的 `tag` 字段决定

## 文件职责
| 文件 | 职责 | 备注 |
|------|------|------|
| utils.py | 常量、数据模型、网络、日志、异常类 | 基础模块，不导入其他项目模块 |
| update_subscriptions.py | 主流程：下载 → 转换 → 合并 → 校验 → 上传 | 入口 main() 在此文件 |
| merge_config.py | 模板合并逻辑、provider 展开、节点正则筛选 | 依赖 utils |
| convert.mjs | Clash → sing-box 批量转换 | Node.js，依赖 deps/proxy-utils.esm.mjs + jsdom |
| ruleset_convert.py | 规则集下载与转换 | 独立 workflow，生成 JSON + SRS，依赖 pyyaml |
| svg.py | 状态图生成（浅色/深色主题）+ sing-box 版本展示 | 依赖 utils |

## 数据模型（utils.py 中定义）

| 数据类 | 不可变 | 说明 |
|--------|--------|------|
| `FlowInfo` | ✅ frozen | 订阅流量信息：upload/download/total/expire |
| `Subscription` | ✅ frozen | 订阅配置：name/url/filename/use_gist/env_name |
| `DownloadResult` | ❌ | 下载结果：status("ok"/"invalid"/"error")/raw_content/is_converted |
| `SubscriptionInfo` | ❌ | 订阅统计：name/flow/node_count/from_backup |
| `HttpResponse` | ❌ | HTTP 响应封装：status_code/text/headers |

关键字段说明：
- `DownloadResult.is_converted=True` 表示内容来自 Gist 备份（已是 sing-box 格式），跳过转换
- `SubscriptionInfo.from_backup=True` 表示数据来自 Gist 备份，SVG 中显示 📦 标记
- `Subscription.use_gist` 由 `GIST_URL*` 环境变量前缀自动决定，控制 provider URL 指向

## 文档分工
- `.clinerules`：AI 编码行为约束（"不要做什么"、"必须做什么"）
- `ARCHITECTURE.md`：项目架构、设计决策及理由（"为什么这样做"）
- 需要记录设计决策/理由时 → 更新 `ARCHITECTURE.md`
- 需要更新编码规范/约束时 → 更新 `.clinerules`

## 特殊目录

### sing-box-reF1nd-docs/
- 用途：sing-box reF1nd 分支的完整文档副本，用于部署到 GitHub Pages 并供 AI 在本地阅读参考
- 不要删除或将其列为可删减项目
- 内容包含：mkdocs 配置、多语言文档、客户端说明、配置参考等

## 代码规范

### 类型注解
- 所有函数必须有完整的类型注解（参数 + 返回值）
- 使用 Python 3.12+ 语法：`list[str]` 而非 `List[str]`，`str | None` 而非 `Optional[str]`

### 日志
- 使用 utils 中的 log_info / log_warn / log_error
- 日志输出到 stderr（避免污染 stdout 数据流）
- stdout 只用于 Node.js 子进程的 JSON 输出

### 异常
- 使用 SubDlError 异常体系（ConfigError / DownloadError / ConversionError / TemplateError / ValidationError / UploadError）
- 不要使用裸 Exception，自定义异常时继承 SubDlError
- 异常应携带 context 字典，包含相关上下文信息

### 网络请求
- 统一使用 utils.http_get_with_retry / http_request
- 不要直接使用 requests 库或 urllib

### 共享可变状态
- `_load_templates()` 返回的模板列表共享公共零件对象（providers/outbounds 等）
- 需要修改模板的函数必须先 `copy.deepcopy()`，不能原地修改
- 修改 `merge_config.py` 中模板数据时同理

## 数据流
```
SUB_URL (env)
  → parse_subscriptions()        # 解析环境变量（SUB_URL* → 常规, GIST_URL* → Gist）
  → _download_all()              # 并行下载订阅
  → _try_gist_fallback()         # 失败时从 Gist 备份获取已转换的 sing-box JSON
  → _convert_batch()             # Node.js 批量转换（跳过 is_converted=True 的）
  → _aggregate_results()         # 聚合结果：收集文件、订阅信息、节点字典
  → _load_templates()            # 加载模块化模板
  → merge_all_templates()        # 合并节点到模板
  → generate_provider_configs()  # 生成 providers 版本（use_gist 的 provider 指向 Gist）
  → _validate_configs()          # sing-box check 校验（official + reF1nd 双二进制）
  → upload_to_gist()             # 清理旧文件 + 上传到 Gist
```

## 模块依赖关系
```
update_subscriptions.py (主入口)
  ├── utils.py         (基础工具，无项目内依赖)
  ├── merge_config.py  (模板合并，依赖 utils)
  ├── svg.py           (SVG 生成，依赖 utils)
  └── convert.mjs      (Node.js，通过 subprocess 调用)

ruleset_convert.py     (独立入口，由 Ruleset Update workflow 调用)
  └── utils.py
```

## 关键设计约束

### stdout/stderr 分离
- stdout 只输出 JSON 数据（Node.js 子进程通信）
- 所有日志必须走 stderr（通过 log_info 等函数）

### Gist 文件名模式
- `_MANAGED_FILE_PATTERNS` 定义了 SubDl 托管的文件名模式：
  - `*.yaml` — 原始订阅
  - `*-singbox.json` — 转换后的 sing-box 节点
  - `sing-box*.json` — 合并配置 + providers 配置
- 清理旧文件时根据此模式判断，不要随意修改

### Provider 前缀机制
- merge_config 步骤 1 中会给所有节点 tag 加上 "{sub_name}/" 前缀
- process_providers 展开 provider 引用时必须使用 "{provider_tag}/{node_tag}" 格式
- 否则 selector 中的引用会指向不存在的节点

### 模板结构
- config_template/ 采用模块化：base / dns / providers / outbounds / route + inbounds/ 变体
- inbounds/ 下每个文件自动生成对应配置
- 新增变体只需在 inbounds/ 目录下添加文件

### Gist 备份容错
- 当原始订阅下载失败时，`_try_gist_fallback()` 尝试从 Gist 获取上次成功转换的 sing-box JSON
- 获取到的备份标记为 `is_converted=True`，跳过 Node.js 转换直接使用
- 仅在所有订阅都下载失败时才会完全中断（抛出 DownloadError）

### 配置校验与清理
- `_validate_configs()` 用 sing-box check 校验所有配置文件
- 校验失败的文件从上传列表中移除（不阻断流程，但以非零退出码告警）
- `_cleanup_old_gist_files()` 删除 Gist 中不再生成的旧托管文件

## 设计决策备忘

以下设计选择是有意为之，AI 不应建议"优化"这些方面。
详见 `ARCHITECTURE.md` "关键设计决策" 章节。

- **运行环境**：完全运行在 GitHub Actions CI 上，本地仅编写代码，不需要 Dry-run/本地预览
- **不做节点去重**：不同订阅不会出现相同节点，去重逻辑无实际收益
- **不做地区分布统计**：模板已通过正则 include/exclude 按地区分组，统计功能属于重复劳动
- **不做订阅过期预警**：机场服务商会在到期前自行发送邮件提醒，无需程序额外处理
- **不做 HTTP 超时可配置化**：当前硬编码的 30 秒超时已覆盖绝大多数场景，增加环境变量收益不大

## 代码变更规范

每次修改代码后，必须检查并更新以下内容：
1. 如果改变了模块职责或文件分工 → 更新本文件中"文件职责"章节
2. 如果新增/删除/重命名了公共函数 → 更新本文件中"文件职责"章节
3. 如果改变了数据流 → 更新本文件中"数据流"章节
4. 如果改变了关键约束 → 更新本文件中"关键设计约束"章节
5. 如果新增/修改了环境变量 → 更新本文件中"环境变量"章节
6. 如果改变了数据模型 → 更新本文件中"数据模型"章节
7. 如果涉及设计决策/理由 → 更新 `ARCHITECTURE.md` "关键设计决策"章节
8. 涉及代码或架构变更时 → 更新 `CHANGELOG.md` 顶部追加记录（纯模板文件变更除外）
9. **在调用 `attempt_completion` 之前，必须逐条对照以上 1~8 条，确认所有需要的更新已实际执行（不是"想到"而是"已写入文件"）。此为硬性前置检查，未通过不得提交结果。**

## 命名规范
- 文件名：snake_case（Python）、camelCase（Node.js）
- 函数名：snake_case
- 常量：UPPER_SNAKE_CASE
- 异常类：PascalCase
- 数据类：PascalCase，使用 @dataclass 装饰器
- 私有函数/变量：_single_underscore 前缀
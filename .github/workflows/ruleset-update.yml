name: Ruleset Update

on:
  push:
    branches: [main]
    paths:
      - 'ruleset/custom_rule/**'
      - 'ruleset/ruleset_source.txt'
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

# 防止多次运行重叠
concurrency:
  group: ruleset-update
  cancel-in-progress: false

# sing-box 二进制通过 actions/cache 按版本号缓存，版本不变时跳过下载

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 1

    - name: Install Python dependencies
      run: pip install -r src/requirements.txt

    - name: Get sing-box version
      id: singbox-ver
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        set -Eeuo pipefail
        VERSION=$(gh api repos/SagerNet/sing-box/releases/latest --jq '.tag_name')
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "✓ sing-box 最新版本: $VERSION"

    - name: Cache sing-box binary
      uses: actions/cache@v6
      id: cache-singbox
      with:
        path: /tmp/sing-box
        key: sing-box-official-${{ steps.singbox-ver.outputs.version }}

    - name: Download sing-box
      if: steps.cache-singbox.outputs.cache-hit != 'true'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        set -Eeuo pipefail
        VER="${{ steps.singbox-ver.outputs.version }}"
        echo "→ 下载 sing-box $VER..."
        # 优先查找 tar.gz，找不到则用 .deb
        TAR_NAME=$(gh api repos/SagerNet/sing-box/releases/tags/$VER --jq '[.assets[].name] | map(select(endswith(".tar.gz"))) | .[0]' 2>/dev/null || true)
        if [ -n "$TAR_NAME" ] && [ "$TAR_NAME" != "null" ]; then
          FULL_URL=$(gh api repos/SagerNet/sing-box/releases/tags/$VER --jq ".assets[] | select(.name == \"$TAR_NAME\") | .browser_download_url")
          curl -fsSL "$FULL_URL" | tar xz -C /tmp/
          mv /tmp/sing-box-*/sing-box /tmp/sing-box
          rm -rf /tmp/sing-box-*/
        else
          DEB_NAME=$(gh api repos/SagerNet/sing-box/releases/tags/$VER --jq '[.assets[].name] | map(select(endswith(".deb"))) | .[0]' 2>/dev/null || true)
          if [ -z "$DEB_NAME" ] || [ "$DEB_NAME" = "null" ]; then
            echo "::error::找不到 sing-box $VER 的二进制 asset"
            exit 1
          fi
          FULL_URL=$(gh api repos/SagerNet/sing-box/releases/tags/$VER --jq ".assets[] | select(.name == \"$DEB_NAME\") | .browser_download_url")
          curl -fsSL "$FULL_URL" -o /tmp/_singbox.deb
          dpkg-deb -x /tmp/_singbox.deb /tmp/_singbox-extract
          find /tmp/_singbox-extract -name sing-box -type f -exec mv {} /tmp/sing-box \;
          rm -rf /tmp/_singbox.deb /tmp/_singbox-extract
        fi
        chmod +x /tmp/sing-box
        echo "✓ sing-box 就绪: $(/tmp/sing-box version 2>/dev/null || echo '未知版本')"

    - name: Build rulesets
      env:
        SING_BOX_BIN: /tmp/sing-box
      run: python src/ruleset_convert.py

    - name: Verify generated files
      run: |
        set -Eeuo pipefail
        JSON_COUNT=$(ls ruleset/json/*.json 2>/dev/null | wc -l)
        SRS_COUNT=$(ls ruleset/srs/*.srs 2>/dev/null | wc -l)
        if [ "$JSON_COUNT" -eq 0 ] || [ "$SRS_COUNT" -eq 0 ]; then
          echo "::error::No generated files found! JSON: $JSON_COUNT, SRS: $SRS_COUNT"
          exit 1
        fi
        echo "✅ Verified: $JSON_COUNT JSON files, $SRS_COUNT SRS files"

    - name: Upload and clean release assets
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        set -Eeuo pipefail

        # ===== 1. 先上传所有新文件（覆盖同名旧文件）=====
        echo "⬆️ Uploading JSON rulesets..."
        gh release upload ruleset-json ruleset/json/*.json --clobber
        echo "⬆️ Uploading SRS rulesets..."
        gh release upload ruleset-srs ruleset/srs/*.srs --clobber
        echo "✅ All files uploaded successfully"

        # ===== 2. 上传全部成功后，清理多余的旧 asset =====
        # 获取当前 release 上的 asset 列表
        EXISTING_JSON=$(gh release view ruleset-json --json assets --jq '.assets[].name')
        EXISTING_SRS=$(gh release view ruleset-srs --json assets --jq '.assets[].name')

        # 获取本地新生成的文件名列表
        LOCAL_JSON=$(ls ruleset/json/*.json | xargs -I{} basename {})
        LOCAL_SRS=$(ls ruleset/srs/*.srs | xargs -I{} basename {})

        # 删除 release 上存在但本地已不存在的 asset
        for asset in $EXISTING_JSON; do
          if ! echo "$LOCAL_JSON" | grep -qxF "$asset"; then
            echo "🗑️ Deleting stale asset: $asset"
            gh release delete-asset ruleset-json "$asset" --yes || true
          fi
        done
        for asset in $EXISTING_SRS; do
          if ! echo "$LOCAL_SRS" | grep -qxF "$asset"; then
            echo "🗑️ Deleting stale asset: $asset"
            gh release delete-asset ruleset-srs "$asset" --yes || true
          fi
        done

        echo "✅ Release assets updated successfully"
